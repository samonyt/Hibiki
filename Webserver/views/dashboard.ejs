<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        <%= client.user.username %>
    </title>
    <link rel="stylesheet" href="https://jenil.github.io/bulmaswatch/materia/bulmaswatch.min.css">
    <script defer src="https://use.fontawesome.com/releases/v5.0.7/js/all.js"></script>
    <link rel="stylesheet" href="/pub/css/bulma.css">
</head>

<body>
    <section class="section">
        <div class="container">
            <div class="navbar-menu">
                <div class="navbar-brand" id="brand">
                    <b id="home-text">
                        <%= client.user.username %>
                    </b>
                </div>
                <div class="navbar-start">
                    <a onclick="location.href='/'" class="navbar-item" id="home">
                        <span class="icon">
                            <i class="fas fa-home" id="icon-home"></i>
                        </span>
                        <span>Home</span>
                    </a>
                    <a onclick="location.href='/stats'" class="navbar-item" id="stats">
                        <span class="icon">
                            <i class="fas fa-desktop"></i>
                        </span>
                        <span>Statistics</span>
                    </a>
                    <a onclick="location.href='/api'" class="navbar-item" id="api">
                        <span class="icon">
                            <i class="fas fa-code"></i>
                        </span>
                        <span>API</span>
                    </a>
                </div>
                <div class="navbar-end">
                    <a onclick="location.href='/logout'" class="button is-warning">
                        <span class="icon">
                            <i class="fas fa-sign-in-alt"></i>
                        </span>
                        <span>Logout</span>
                    </a>
                </div>
            </div>
            <h1 class="title">
                Welcome, <%= user.username %>.
            </h1>
            <p class="subtitle">
                Use the navigation bar to navigate through the website.
            </p>
            <% const guilds = user.guilds.filter(g => modules.Perms.convertPerms(g.permissions)["MANAGE_GUILD"] === true).filter(g => client.guilds.get(g.id)) %>
                <% const gj = guilds.map(g => g.name) %>
                    <% if (gj.length === 0) { %>
                        <p id="no-guilds">
                            <strong>
                                <i class="far fa-times-circle"></i> You have no guilds to manage. :(</strong>
                        </p>
                        <% } else { %>
                            <nav class="panel">
                                <p class="panel-tabs">
                                    <a class="is-active" id="server-tab">Servers</a>
                                </p>
                                <a class="panel-block" id="panel">
                                    <span class="panel-icon" id="icon-panel">
                                        <i class="fas fa-book" aria-hidden="true"></i>
                                    </span>
                                    <p>
                                        <%= gj %>
                                    </p>
                                </a>
                            </nav>
                            <% } %>
        </div>
    </section>
</body>

</html>